@model IEnumerable<MinecraftWrapper.Data.WhiteListEntry>

    @{
    ViewData["Title"] = "View";
    Layout = "~/Views/Shared/_Layout.cshtml";
    }

    <h2>View</h2>

    <p>
        <a asp-action="Create">Create New</a>
    </p>
    <table class="table">
        <thead>
            <tr>
                <th>
                    @Html.DisplayNameFor(model => model.name)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.xuid)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.ignoresPlayerLimit)
                </th>
                <th>
                    Actions
                </th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model) {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.name)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.xuid)
                </td>
                <td>
                    @Html.EditorFor(modelItem =>item.ignoresPlayerLimit)
                </td>
                <td>
                    @Html.ActionLink("Delete", "DeleteWhiteListEntry", new { item.name })
                </td>
            </tr>
}
        </tbody>
    </table>

<form action="AddWhiteListEntry" method="post">
    <label>Name</label>
    <input type="text" name="name" />
    <input type="submit" value="Add"/>
</form>
