@model IEnumerable<MinecraftWrapper.Models.AuthorizationKey>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.AuthorizationToken)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.User)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.AuthorizationToken)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.User.Id)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.AuthorizationKeyId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.AuthorizationKeyId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.AuthorizationKeyId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
